let jwt;_f2a‍.x([["default",()=>_f2a‍.o]]);_f2a‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
    const token = ctx.cookies.get('access_token');
    if ( !token ) return next();
    try {
        const decoded = jwt.verify(token, process.env.JWT_SECRET);
        ctx.state.user = {
            _id: decoded._id,
            username: decoded.username
        };
        _f2a‍.g.console.log(decoded);
        return next();
    } catch(e) {
        // 토큰 검증 실패
        return next();
    }
};

_f2a‍.d(jwtMiddleware);